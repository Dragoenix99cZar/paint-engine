[package]
name = "paint-engine"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
# Restrict default features to reduce WASM binary size
image = { version = "0.25", default-features = false, features = ["png", "jpeg", "webp"] }
